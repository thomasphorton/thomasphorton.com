version: 0.2
            
phases:
  install:
    commands:
      - npm install hexo -g
      - npm install
  build:
    commands:
      - hexo generate
      - chmod +x './deployment/deploy-to-s3.sh' && './deployment/deploy-to-s3.sh'
